const goodluck = {
    complex: 'fair',
    height: 'short',
    status: 'married',
    husband: 'victor'
}

Object.entries(goodluck).map(([prop, value]) => {
console.log(`Goodluck has the following ${prop} : ${value}`)
})